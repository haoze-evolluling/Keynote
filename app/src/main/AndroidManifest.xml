<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <queries>
        <intent>
            <action android:name="android.intent.action.SET_ALARM" />
        </intent>
    </queries>

    <application
        android:name=".KeyNoteApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KeyNote">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.KeyNote.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NotesActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".EditNoteActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".BillActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".BillStatsActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".AaSplitActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".HabitActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".SettingsActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".AiProviderManageActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".TodoActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".TagNotesActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".KnowledgeVaultActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".DateGroupNotesActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".TrashActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".DataExportActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ScheduleActivity" android:windowSoftInputMode="adjustResize" />

        <receiver
            android:name=".receiver.TrashCleanupReceiver"
            android:exported="false" />
        <receiver
            android:name=".receiver.AlarmReceiver"
            android:exported="false" />
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>

